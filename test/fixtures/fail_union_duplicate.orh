module fixture
#name    = "fail_union_dup"
#version = (1, 0, 0)
#build   = exe

pub struct A { pub x: i32 }
pub struct B { pub y: i32 }

// B appears in both sides — should error after flattening
func bad(val: ((A | B) | (B | A))) void {
}

func main() void {
}
