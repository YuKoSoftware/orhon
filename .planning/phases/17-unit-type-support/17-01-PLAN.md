---
phase: 17-unit-type-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/tester.orh
  - test/fixtures/tester_main.orh
  - test/10_runtime.sh
  - src/templates/example/error_handling.orh
autonomous: true
requirements: [TAMGA-03]

must_haves:
  truths:
    - "User-defined function with (Error | void) return type compiles and runs"
    - "is Error check on (Error | void) result works at runtime"
    - "Bare return from (Error | void) function produces void success"
    - "Error return from (Error | void) function produces error"
    - "Existing void functions and (Error | i32) functions still work unchanged"
  artifacts:
    - path: "test/fixtures/tester.orh"
      provides: "User-defined (Error | void) functions"
      contains: "(Error | void)"
    - path: "test/fixtures/tester_main.orh"
      provides: "Call-site tests for (Error | void) functions"
      contains: "error_void_ok"
    - path: "test/10_runtime.sh"
      provides: "Runtime test entries for error_void tests"
      contains: "error_void_ok"
    - path: "src/templates/example/error_handling.orh"
      provides: "Example module coverage of (Error | void)"
      contains: "(Error | void)"
  key_links:
    - from: "test/fixtures/tester.orh"
      to: ".orh-cache/generated/tester.zig"
      via: "codegen emits anyerror!void"
      pattern: "anyerror!void"
    - from: "test/fixtures/tester_main.orh"
      to: "test/fixtures/tester.orh"
      via: "cross-module call to (Error | void) function"
      pattern: "tester\\.do_side_effect"
---

<objective>
Add test coverage and example module documentation for `(Error | void)` — the pipeline already supports it (confirmed by research tracing all 12 passes), but no user-written test fixture exercises it end-to-end.

Purpose: Close TAMGA-03 — Tamga needs `(Error | void)` for bridge functions that can fail but return nothing on success. Verify it works in user code, not just stdlib bridge declarations.
Output: Test fixtures, runtime test entries, example module update, full green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unit-type-support/17-RESEARCH.md

@test/fixtures/tester.orh
@test/fixtures/tester_main.orh
@test/10_runtime.sh
@src/templates/example/error_handling.orh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add (Error | void) test fixtures and runtime entries</name>
  <files>test/fixtures/tester.orh, test/fixtures/tester_main.orh, test/10_runtime.sh</files>
  <read_first>
    - test/fixtures/tester.orh (lines 392-417 for existing error handling pattern)
    - test/fixtures/tester_main.orh (lines 620-638 for how to add new test calls)
    - test/10_runtime.sh (lines 31-56 for TEST_NAME list)
  </read_first>
  <action>
    Add user-defined `(Error | void)` functions to `test/fixtures/tester.orh`, following the existing error handling section pattern (per D-01, D-02, D-08, D-11).

    In `test/fixtures/tester.orh`, after the `IsTestType` struct at line 1424, add a new section:

    ```
    // ─── Error | void ───────────────────────────────────────────

    pub func do_side_effect(flag: bool) (Error | void) {
        if(flag) {
            return Error("side effect failed")
        }
        return
    }

    pub func test_error_void_ok() i32 {
        const result = do_side_effect(false)
        if(result is Error) {
            return 0
        }
        return 1
    }

    pub func test_error_void_fail() i32 {
        const result = do_side_effect(true)
        if(result is Error) {
            return 1
        }
        return 0
    }
    ```

    In `test/fixtures/tester_main.orh`, before the `console.println("TESTER:DONE")` line, add two new test blocks:

    ```
    // Test: (Error | void) — success path returns void, no error
    if(tester.test_error_void_ok() == 1) {
        console.println("PASS error_void_ok")
    } else {
        console.println("FAIL error_void_ok")
    }

    // Test: (Error | void) — error path returns error
    if(tester.test_error_void_fail() == 1) {
        console.println("PASS error_void_fail")
    } else {
        console.println("FAIL error_void_fail")
    }
    ```

    In `test/10_runtime.sh`, add `error_void_ok error_void_fail` to the TEST_NAME list, on the line after `is_qualified is_not_qualified` (before the semicolon closing the list).
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && bash test/09_language.sh && bash test/10_runtime.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "(Error | void)" test/fixtures/tester.orh` succeeds
    - `grep -q "error_void_ok" test/fixtures/tester_main.orh` succeeds
    - `grep -q "error_void_fail" test/fixtures/tester_main.orh` succeeds
    - `grep -q "error_void_ok" test/10_runtime.sh` succeeds
    - `bash test/10_runtime.sh` shows "PASS error_void_ok" and "PASS error_void_fail"
  </acceptance_criteria>
  <done>Two new runtime tests pass: error_void_ok (success path returns void) and error_void_fail (error path returns error). Codegen emits anyerror!void for the do_side_effect function.</done>
</task>

<task type="auto">
  <name>Task 2: Update example module and run full test suite</name>
  <files>src/templates/example/error_handling.orh</files>
  <read_first>
    - src/templates/example/error_handling.orh (full file — pattern for error handling examples)
  </read_first>
  <action>
    Add an `(Error | void)` example to `src/templates/example/error_handling.orh`, following the existing pattern. This is the living language manual — it must cover every implemented feature (per CLAUDE.md example module rules, and per D-01, D-02).

    After the `arb_union_check` function and before the `// ─── Tests ───` section, add:

    ```
    // ─── Error Union with Void ──────────────────────────────────────────────────

    func validate_input(value: i32) (Error | void) {
        if(value < 0) {
            return Error("negative value")
        }
        return
    }

    func check_and_report(value: i32) i32 {
        const result = validate_input(value)
        if(result is Error) {
            return 0
        }
        return 1
    }
    ```

    Update the tests section at the bottom of the file — add a new test block after "null handling":

    ```
    test "error void" {
        assert(check_and_report(5) == 1)
        assert(check_and_report(0 - 1) == 0)
    }
    ```

    Then run the full test suite to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "(Error | void)" src/templates/example/error_handling.orh` succeeds
    - `grep -q "validate_input" src/templates/example/error_handling.orh` succeeds
    - `grep -q 'test "error void"' src/templates/example/error_handling.orh` succeeds
    - `./testall.sh` completes with 0 failures across all 11 stages
  </acceptance_criteria>
  <done>Example module documents (Error | void) pattern. Full test suite passes — all 11 stages green. No regressions in existing void or error union functions.</done>
</task>

</tasks>

<verification>
1. `grep -q "anyerror!void" .orh-cache/generated/tester.zig` — codegen emits correct Zig type (run after test/09_language.sh)
2. `bash test/10_runtime.sh` — PASS error_void_ok and PASS error_void_fail appear
3. `./testall.sh` — all 11 stages pass with 0 failures
4. Existing error union tests (error_ok, error_fail) still pass — no regression
</verification>

<success_criteria>
- `(Error | void)` compiles in user-written Orhon code and emits `anyerror!void` in generated Zig
- Success path (bare return) and error path both work at runtime
- `is Error` check on `(Error | void)` result works correctly
- Example module covers the feature for `orhon init` projects
- Full test suite green — zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/17-unit-type-support/17-01-SUMMARY.md`
</output>
