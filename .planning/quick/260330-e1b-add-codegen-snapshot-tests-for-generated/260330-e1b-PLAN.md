---
phase: quick-260330-e1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/snapshots/snap_basics.orh
  - test/snapshots/snap_basics_main.orh
  - test/snapshots/snap_structs.orh
  - test/snapshots/snap_structs_main.orh
  - test/snapshots/snap_control.orh
  - test/snapshots/snap_control_main.orh
  - test/snapshots/snap_errors.orh
  - test/snapshots/snap_errors_main.orh
  - test/snapshots/expected/snap_basics.zig
  - test/snapshots/expected/snap_structs.zig
  - test/snapshots/expected/snap_control.zig
  - test/snapshots/expected/snap_errors.zig
  - test/08_codegen.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "Codegen snapshot tests run as part of test/08_codegen.sh"
    - "Each snapshot fixture compiles and produces a .zig file that is diffed against expected output"
    - "A codegen regression (changed .zig output) causes a test failure with a readable diff"
    - "Snapshots cover variables, functions, structs, enums, control flow, and error unions"
  artifacts:
    - path: "test/snapshots/snap_basics.orh"
      provides: "Fixture covering variables, functions, compt, type aliases"
    - path: "test/snapshots/snap_structs.orh"
      provides: "Fixture covering structs, enums, methods, bitfields"
    - path: "test/snapshots/snap_control.orh"
      provides: "Fixture covering if/else, match, loops, break, continue, defer"
    - path: "test/snapshots/snap_errors.orh"
      provides: "Fixture covering error unions, null unions, throw"
    - path: "test/snapshots/expected/"
      provides: "Expected .zig output files for diff comparison"
    - path: "test/08_codegen.sh"
      provides: "Updated test script with snapshot diff section"
  key_links:
    - from: "test/08_codegen.sh"
      to: "test/snapshots/expected/*.zig"
      via: "diff command comparing generated vs expected"
      pattern: "diff.*expected.*generated"
---

<objective>
Add codegen snapshot tests that capture generated .zig output for representative .orh programs and diff against expected .zig baselines. This catches subtle codegen regressions that runtime tests miss — unnecessary allocations, wrong variable names, missing defer, changed import patterns, etc.

Purpose: Complement runtime correctness tests (test/10_runtime.sh) with output-level codegen verification. When codegen changes, the diff shows exactly what changed.
Output: 4 snapshot fixtures, 4 expected .zig baselines, updated test/08_codegen.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@test/08_codegen.sh
@test/helpers.sh
@test/fixtures/tester.orh (reference for Orhon syntax covering all features)
@test/fixtures/multi_null_union.orh (reference for multi-union syntax)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create snapshot fixture .orh files</name>
  <files>
    test/snapshots/snap_basics.orh
    test/snapshots/snap_basics_main.orh
    test/snapshots/snap_structs.orh
    test/snapshots/snap_structs_main.orh
    test/snapshots/snap_control.orh
    test/snapshots/snap_control_main.orh
    test/snapshots/snap_errors.orh
    test/snapshots/snap_errors_main.orh
  </files>
  <action>
Create 4 small, focused .orh fixture pairs (module + main) in `test/snapshots/`. Each pair is a minimal two-module project (a feature module + a main module that imports it). Keep fixtures SHORT (20-40 lines each for the feature module) — snapshot diffs must be readable.

Each fixture pair needs a main module with `#name`, `#version`, `#build = exe`, and a trivial `func main() void {}` that imports the feature module. The feature module contains the actual codegen targets.

**snap_basics.orh** (module snap_basics): Tests basic codegen paths.
- 2 const declarations (one i32, one String)
- 1 var declaration with assignment
- 1 public function with parameters and return
- 1 compt function (generates `inline fn`)
- 1 type alias (`const Speed: type = i32`)

**snap_basics_main.orh** (module main): `#name = "snap_basics"`, `#version = Version(1,0,0)`, `#build = exe`, `import snap_basics`, trivial `func main() void {}`.

**snap_structs.orh** (module snap_structs): Tests struct/enum codegen.
- 1 pub struct with 2-3 fields (including pub fields)
- 1 struct with a method (const &Self parameter)
- 1 struct with default field values
- 1 enum(u8) with 3 variants
- 1 enum with a method

**snap_structs_main.orh**: Same pattern as snap_basics_main but `#name = "snap_structs"`, imports snap_structs.

**snap_control.orh** (module snap_control): Tests control flow codegen.
- 1 function with if/elif/else
- 1 function with match statement (3 arms + default)
- 1 function with for loop over a slice parameter
- 1 function with while loop + break
- 1 function using defer

**snap_control_main.orh**: Same pattern, `#name = "snap_control"`, imports snap_control.

**snap_errors.orh** (module snap_errors): Tests error/null union codegen.
- 1 function returning `(Error | i32)` with both paths
- 1 function returning `(null | i32)` with both paths
- 1 function using `throw` for error propagation
- 1 function demonstrating `is` type check on null union

**snap_errors_main.orh**: Same pattern, `#name = "snap_errors"`, imports snap_errors.

All functions should be `pub` in the feature modules so codegen emits `pub fn`. Keep function bodies minimal — just enough to exercise the codegen path, not to test runtime correctness.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ls test/snapshots/snap_*.orh | wc -l</automated>
  </verify>
  <done>8 .orh fixture files exist in test/snapshots/ (4 feature modules + 4 main modules)</done>
</task>

<task type="auto">
  <name>Task 2: Generate expected .zig baselines and add snapshot diff to test/08_codegen.sh</name>
  <files>
    test/snapshots/expected/snap_basics.zig
    test/snapshots/expected/snap_structs.zig
    test/snapshots/expected/snap_control.zig
    test/snapshots/expected/snap_errors.zig
    test/08_codegen.sh
  </files>
  <action>
**Step 1: Generate baselines.** For each of the 4 fixture pairs, set up a temp project directory, copy in the fixture pair, run `orhon build`, and copy the generated `.zig` file for the feature module from `.orh-cache/generated/{module_name}.zig` to `test/snapshots/expected/{module_name}.zig`. Do this by hand (or script) to produce the initial expected files.

The process for each fixture (e.g., snap_basics):
```
mkdir -p /tmp/orhon-snap/src
cp test/snapshots/snap_basics.orh /tmp/orhon-snap/src/snap_basics.orh
cp test/snapshots/snap_basics_main.orh /tmp/orhon-snap/src/main.orh
cd /tmp/orhon-snap
$ORHON build
cp .orh-cache/generated/snap_basics.zig $REPO/test/snapshots/expected/snap_basics.zig
```

Repeat for all 4. If any fixture fails to compile, fix the fixture first.

**Step 2: Update test/08_codegen.sh.** Add a new section at the end (before `report_results`) titled "Codegen snapshot tests". The section should:

1. For each of the 4 snapshot fixtures:
   a. Create a project directory in $TESTDIR
   b. Copy the fixture pair into src/
   c. Run `$ORHON build` (suppress output, allow failure)
   d. Compare the generated .zig with the expected .zig using `diff -u`
   e. If diff is empty: `pass "snapshot: {name}"`
   f. If diff is non-empty: `fail "snapshot: {name}" "run: diff test/snapshots/expected/{name}.zig {generated_path}"` and print the first 20 lines of the diff for context

Use a shell function to avoid repeating the same logic 4 times:

```bash
# ── Codegen snapshot tests ──────────────────────────────────
snapshot_test() {
    local name="$1"
    local projdir="$TESTDIR/snap_${name}"
    mkdir -p "$projdir/src"
    cp "$REPO_DIR/test/snapshots/snap_${name}.orh" "$projdir/src/snap_${name}.orh"
    cp "$REPO_DIR/test/snapshots/snap_${name}_main.orh" "$projdir/src/main.orh"
    cd "$projdir"
    "$ORHON" build >/dev/null 2>&1 || true

    local generated=".orh-cache/generated/snap_${name}.zig"
    local expected="$REPO_DIR/test/snapshots/expected/snap_${name}.zig"

    if [ ! -f "$generated" ]; then
        fail "snapshot: $name" "generated file not found"
        return
    fi

    local diff_out
    diff_out=$(diff -u "$expected" "$generated" 2>&1) || true
    if [ -z "$diff_out" ]; then
        pass "snapshot: $name"
    else
        fail "snapshot: $name" "codegen output differs from expected"
        echo "$diff_out" | head -20
    fi
}

snapshot_test "basics"
snapshot_test "structs"
snapshot_test "control"
snapshot_test "errors"
```

This keeps the test DRY. The function handles project setup, compilation, and diff in one place.

**Important:** Place the snapshot section AFTER the existing "Specific codegen patterns" and "Bare call discard" sections but BEFORE `report_results`. Add a `section "Codegen snapshots"` header for clear test output grouping.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && bash test/08_codegen.sh</automated>
  </verify>
  <done>All 4 snapshot tests pass in test/08_codegen.sh. Expected .zig files exist in test/snapshots/expected/. Full test suite (./testall.sh) still passes.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm nothing is broken:
```bash
./testall.sh
```

Verify snapshot tests catch regressions by temporarily modifying a fixture and confirming the test fails, then revert.
</verification>

<success_criteria>
- 4 snapshot fixture pairs in test/snapshots/ covering basics, structs, control flow, error unions
- 4 expected .zig baselines in test/snapshots/expected/
- test/08_codegen.sh includes snapshot diff section with pass/fail reporting
- All snapshot tests pass (generated matches expected)
- Full test suite passes
- A codegen change that alters .zig output for any fixture causes a visible test failure with diff context
</success_criteria>

<output>
After completion, create `.planning/quick/260330-e1b-add-codegen-snapshot-tests-for-generated/260330-e1b-SUMMARY.md`
</output>
