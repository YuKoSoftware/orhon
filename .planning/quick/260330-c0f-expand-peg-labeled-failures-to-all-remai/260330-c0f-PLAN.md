---
phase: quick-260330-c0f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/peg/orhon.peg
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "All user-facing grammar rules produce human-readable error messages on parse failure"
    - "Existing 14 labels remain unchanged"
    - "All tests pass after adding new labels"
  artifacts:
    - path: "src/peg/orhon.peg"
      provides: "Complete labeled failures coverage"
      contains: "{label:"
  key_links:
    - from: "src/peg/orhon.peg"
      to: "src/peg/grammar.zig"
      via: "grammar parser reads {label: ...} annotations"
      pattern: "tryParseLabel"
---

<objective>
Add {label: "..."} annotations to all remaining grammar rules in orhon.peg that would produce user-facing parse errors.

Purpose: When a parse fails, the engine reports "expected {label}, found '{token}'" instead of cryptic internal rule names. The infrastructure already exists (v0.10.24) with 14 rules labeled. This task completes coverage for all remaining rules where a failure message is user-visible.

Output: Updated orhon.peg with comprehensive label annotations.
</objective>

<execution_context>
@/home/yunus/.claude/get-shit-done/workflows/execute-plan.md
@/home/yunus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/peg/orhon.peg
@src/peg/grammar.zig (label parsing infrastructure)
@src/peg/engine.zig (furthest_label tracking)
@src/module.zig:475-498 (error message formatting using labels)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add labeled failures to all remaining grammar rules</name>
  <files>src/peg/orhon.peg</files>
  <action>
Add {label: "..."} annotations to the following rules in orhon.peg. Labels go at the END of the rule's last alternative, on the same line as the last token (this is how the grammar parser finds them).

IMPORTANT: Labels ONLY work on the LAST alternative of a rule (the grammar parser reads one label per rule name, placed after the entire rule body). For rules with multiple alternatives using /, the label goes after the final alternative. This is how the existing labels work (see const_decl, var_decl, import_decl).

Rules to label (grouped by section):

**PROGRAM STRUCTURE:**
- metadata: {label: "metadata directive"}
- import_decl first alt already lacks label but second has one — the label on the second alt already covers the rule. Leave as-is OR move label to apply to entire rule. Actually, looking at existing pattern: import_decl has label only on the `use` alt. Add label to the `import` alt too:
  Change: `'import' import_path ('as' IDENTIFIER)? NL` to `'import' import_path ('as' IDENTIFIER)? NL  {label: "import declaration"}`
  BUT labels only work on last alternative. The existing label on `use` IS the last alt, so it already works. Skip this — the label on the last alt covers the rule.

**TOP-LEVEL DECLARATIONS:**
- bitfield_decl: {label: "bitfield declaration"}
- bridge_decl: {label: "bridge declaration"}
- test_decl: {label: "test declaration"}
- thread_decl: {label: "thread declaration"}
- compt_decl: {label: "compt declaration"}

**STATEMENTS:**
- throw_stmt: {label: "throw statement"}
- defer_stmt: {label: "defer statement"}
- break_stmt: {label: "break statement"}
- continue_stmt: {label: "continue statement"}
- match_arm: {label: "match arm"}

**EXPRESSIONS:**
- expr: {label: "expression"}
- compiler_func: {label: "compiler function"}
- array_literal: {label: "array literal"}
- tuple_literal: {label: "tuple literal"}
- error_literal: {label: "error literal"}
- struct_expr: {label: "struct expression"}

**TYPES:**
- borrow_type: {label: "borrow type"}
- ref_type: {label: "reference type"}
- slice_type: {label: "slice type"}
- array_type: {label: "array type"}
- func_type: {label: "function type"}
- generic_type: {label: "generic type"}
- paren_type last alt: {label: "parenthesized type"}

**OTHER:**
- field_decl: {label: "field declaration"}
- param: {label: "parameter"}
- doc_block: {label: "doc comment"}

Do NOT label:
- Internal/helper rules that are never the "furthest rule" in practice (expr precedence tower internals like or_expr, and_expr, add_expr, mul_expr etc. — these bubble up to expr)
- Token-level terminals (IDENTIFIER, INT_LITERAL, etc.)
- Whitespace/comment rules (_)
- Lookahead-only rules (top_level_start)
- error_skip (recovery rule, not user-facing)
- Rules that are always consumed as part of a parent (arg_list, param_list, enum_body, struct_body etc.)

Exact placement for each: append `  {label: "..."}` after the last token/rule reference on the final alternative line, before any trailing comment. Follow the existing spacing convention (two spaces before the annotation).
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh</automated>
  </verify>
  <done>All remaining user-facing grammar rules have {label: "..."} annotations. Total labeled rules increases from 14 to ~38. All tests pass — labels are grammar-only metadata with no behavioral change to parsing.</done>
</task>

</tasks>

<verification>
- grep -c '{label:' src/peg/orhon.peg shows 35+ labels (up from 14)
- ./testall.sh passes (all 11 test stages)
- zig build test passes (unit tests including grammar label parsing tests)
</verification>

<success_criteria>
All grammar rules that produce user-visible parse errors have human-readable labels. No test regressions.
</success_criteria>

<output>
After completion, create `.planning/quick/260330-c0f-expand-peg-labeled-failures-to-all-remai/260330-c0f-SUMMARY.md`
</output>
