---
phase: quick-260330-ck9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zig_runner/zig_runner.zig
  - src/interface.zig
  - test/06_library.sh
autonomous: true
requirements: [TODO-lib-output]
must_haves:
  truths:
    - "Static library .a files are output to lib/ not bin/"
    - "Dynamic library .so files are output to lib/ not bin/"
    - "Interface .orh files are output to lib/ alongside the library"
    - "Executables still output to bin/ unchanged"
    - "All existing tests pass with updated paths"
  artifacts:
    - path: "src/zig_runner/zig_runner.zig"
      provides: "Library artifact copy to lib/ instead of bin/"
    - path: "src/interface.zig"
      provides: "Interface file generation to lib/ instead of bin/"
    - path: "test/06_library.sh"
      provides: "Updated test expectations for lib/ paths"
  key_links:
    - from: "src/zig_runner/zig_runner.zig"
      to: "lib/"
      via: "makePath + copyFile"
      pattern: "makePath.*lib"
    - from: "src/interface.zig"
      to: "lib/"
      via: "makePath + createFile"
      pattern: "lib/.*\\.orh"
---

<objective>
Move compiled library output (.so/.dll/.a) and interface files (.orh) from bin/ to lib/.

Purpose: Libraries cluttering bin/ alongside executables is confusing. Separating them into lib/ follows standard Unix conventions and was requested via Tamga feedback (docs/TODO.md).

Output: Libraries and their interface files go to lib/, executables stay in bin/.
</objective>

<execution_context>
@/home/yunus/.claude/get-shit-done/workflows/execute-plan.md
@/home/yunus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/zig_runner/zig_runner.zig
@src/interface.zig
@test/06_library.sh
</context>

<interfaces>
<!-- Key code locations that need modification -->

From src/zig_runner/zig_runner.zig — buildWithType() lines 200-243:
- `is_lib` detection: `!std.mem.eql(u8, build_type, "exe")`
- Library dst: `try std.fmt.allocPrint(self.allocator, "bin/lib{s}{s}", .{ project_name, ext })`
- Exe dst: `try std.fs.path.join(self.allocator, &.{ "bin", project_name })`
- `try std.fs.cwd().makePath("bin");`
- Print: `std.debug.print("Built: {s}\n", .{dst_name});`

From src/zig_runner/zig_runner.zig — buildAll() lines 116-137:
- Same pattern: `try std.fs.cwd().makePath("bin");`
- Library dst: `try std.fmt.allocPrint(self.allocator, "bin/lib{s}{s}", .{ t.project_name, ext })`
- Exe dst: `try std.fs.path.join(self.allocator, &.{ "bin", t.project_name })`

From src/interface.zig — generateInterface() lines 269-276:
- `try std.fs.cwd().makePath("bin");`
- `const path = try std.fmt.allocPrint(alloc, "bin/{s}.orh", .{binary_name});`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Change library and interface output paths from bin/ to lib/</name>
  <files>src/zig_runner/zig_runner.zig, src/interface.zig</files>
  <action>
In src/zig_runner/zig_runner.zig, function `buildWithType()` (line ~200-243):
- Change the library destination path from `"bin/lib{s}{s}"` to `"lib/lib{s}{s}"` (two occurrences: dst_name format string).
- Change the `makePath("bin")` call to `makePath("lib")` ONLY for the library path. The exe path still needs `makePath("bin")`.
- Split the makePath call: if `is_lib` then `makePath("lib")`, else `makePath("bin")`.
- The print statement already uses `dst_name` so it will automatically show the correct path.

In src/zig_runner/zig_runner.zig, function `buildAll()` (line ~116-137):
- Change the library destination path from `"bin/lib{s}{s}"` to `"lib/lib{s}{s}"`.
- The exe destination stays as `"bin"`.
- Before the loop, create both directories: `makePath("bin")` stays, add `makePath("lib")` (or just create both unconditionally since the loop handles mixed target types).

In src/interface.zig, function `generateInterface()` (line ~269-276):
- Change `makePath("bin")` to `makePath("lib")`.
- Change the path format from `"bin/{s}.orh"` to `"lib/{s}.orh"`.

Do NOT change any executable output paths — those stay in bin/.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <done>Library artifacts (.a, .so) destination is lib/, interface .orh destination is lib/, exe destination unchanged at bin/</done>
</task>

<task type="auto">
  <name>Task 2: Update library test expectations for lib/ paths</name>
  <files>test/06_library.sh</files>
  <action>
In test/06_library.sh, update all path expectations from bin/ to lib/ for library outputs:

Static library section:
- `"Built: bin/libtestlib.a"` -> `"Built: lib/libtestlib.a"`
- `bin/libtestlib.a` -> `lib/libtestlib.a` (file existence check)
- `bin/testlib.orh` -> `lib/testlib.orh` (interface file checks, 3 occurrences)

Dynamic library section:
- `"Built: bin/libtestlib.so"` -> `"Built: lib/libtestlib.so"`
- `bin/libtestlib.so` -> `lib/libtestlib.so`
- `bin/testlib.orh` -> `lib/testlib.orh`

Interface file import section:
- `bin/ifacelib.orh` -> `lib/ifacelib.orh` (2 occurrences: existence check and cp command)

Do NOT change any executable-related paths.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && bash test/06_library.sh 2>&1 | tail -20</automated>
  </verify>
  <done>All 06_library.sh tests pass with lib/ paths for library and interface outputs</done>
</task>

</tasks>

<verification>
```bash
cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -30
```
All 11 test stages pass. Libraries go to lib/, executables go to bin/.
</verification>

<success_criteria>
- `orhon build` with `#build = static` produces `lib/lib<name>.a` and `lib/<name>.orh`
- `orhon build` with `#build = dynamic` produces `lib/lib<name>.so` and `lib/<name>.orh`
- `orhon build` with `#build = exe` still produces `bin/<name>`
- All tests in `./testall.sh` pass
</success_criteria>

<output>
After completion, create `.planning/quick/260330-ck9-move-dynamic-library-output-to-separate-/260330-ck9-SUMMARY.md`
</output>
