---
phase: 10-compatibility-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/yunus/Projects/orhon/tamga/src/example/data_types.orh
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03]

must_haves:
  truths:
    - "Tamga data_types.orh uses new Ptr syntax and compiles"
    - "Example module has no .cast() or old Ptr(T, &x) syntax"
    - "Tester fixtures have no .cast() except the negative test"
    - "docs/09-memory.md documents auto-borrow and new Ptr syntax"
    - "docs/05-functions.md has no stale .cast() references"
  artifacts:
    - path: "/home/yunus/Projects/orhon/tamga/src/example/data_types.orh"
      provides: "Updated Ptr syntax on line 85"
      contains: "const p: Ptr(i32) = &x"
  key_links:
    - from: "/home/yunus/Projects/orhon/tamga/src/example/data_types.orh"
      to: "orhon build"
      via: "compiler pipeline"
      pattern: "const p: Ptr\\(i32\\) = &x"
---

<objective>
Update Tamga companion project for new Ptr syntax and verify all compiler fixtures and docs are current after Phases 8-9 changes.

Purpose: COMP-01/02/03 — ensure everything outside the compiler core reflects the two breaking changes (const auto-borrow + Ptr syntax simplification).
Output: Tamga compiles with new syntax, all fixtures and docs verified current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-compatibility-updates/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tamga Ptr syntax and rebuild</name>
  <files>/home/yunus/Projects/orhon/tamga/src/example/data_types.orh</files>
  <read_first>
    - /home/yunus/Projects/orhon/tamga/src/example/data_types.orh (line 85 — old Ptr(i32, &x) syntax)
    - /home/yunus/Projects/orhon/tamga/src/TamgaVK3D/tamga_vk3d.orh (line 6 — Ptr(u8) param type, should be fine as-is)
  </read_first>
  <action>
    Per D-01 and D-03: Update Tamga's data_types.orh line 85.

    Change:
      const p: Ptr(i32) = Ptr(i32, &x)
    To:
      const p: Ptr(i32) = &x

    Per D-01: tamga_vk3d.orh line 6 uses `Ptr(u8)` as a type annotation in a function parameter — this is valid new syntax. No change needed.

    Per D-04: After the edit, delete Tamga's `.orh-cache/` directory and rebuild from scratch:
      rm -rf /home/yunus/Projects/orhon/tamga/.orh-cache
      cd /home/yunus/Projects/orhon/tamga && /home/yunus/Projects/orhon/orhon_compiler/zig-out/bin/orhon build

    If the compiler binary doesn't exist yet, build it first:
      cd /home/yunus/Projects/orhon/orhon_compiler && zig build

    The build must succeed with no errors.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build && rm -rf /home/yunus/Projects/orhon/tamga/.orh-cache && cd /home/yunus/Projects/orhon/tamga && /home/yunus/Projects/orhon/orhon_compiler/zig-out/bin/orhon build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Ptr(i32, &x)" /home/yunus/Projects/orhon/tamga/src/example/data_types.orh returns 0
    - grep -c "const p: Ptr(i32) = &x" /home/yunus/Projects/orhon/tamga/src/example/data_types.orh returns 1
    - Tamga `orhon build` exits 0 with clean cache
  </acceptance_criteria>
  <done>Tamga data_types.orh updated to new Ptr syntax, cache cleared, clean rebuild succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Verify fixtures and docs are current</name>
  <files>None modified — verification only</files>
  <read_first>
    - /home/yunus/Projects/orhon/orhon_compiler/docs/09-memory.md (Ptr + auto-borrow docs)
    - /home/yunus/Projects/orhon/orhon_compiler/docs/05-functions.md (copy/move semantics)
    - /home/yunus/Projects/orhon/orhon_compiler/src/templates/example/data_types.orh (Ptr examples)
  </read_first>
  <action>
    Per D-05, D-06, D-07, D-08, D-09, D-10: Verify all fixtures and docs are current.

    Verification checklist:
    1. COMP-02 — Example module: grep for `.cast(` and `Ptr(.*,` in src/templates/ — must find 0 matches
    2. COMP-02 — Tester fixtures: grep for `.cast(` in test/fixtures/ — only match should be fail_ptr_cast.orh (negative test)
    3. COMP-03 — docs/09-memory.md: confirm it documents (a) `const p: Ptr(T) = &x` syntax, (b) const auto-borrow behavior, (c) no .cast() references
    4. COMP-03 — docs/05-functions.md: confirm no stale `.cast()` or old Ptr constructor references
    5. COMP-02 — Example module demonstrates Ptr(T) with new `&x` syntax (already present in data_types.orh)

    If any doc has stale `.cast()` references, update them. If example module is missing const auto-borrow demo, use discretion on whether to add one (per Claude's Discretion).

    Run ./testall.sh to confirm nothing regressed.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && grep -rc "\.cast(" src/templates/ test/fixtures/ | grep -v ":0$" | grep -v "fail_ptr_cast" ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc "\.cast(" src/templates/ returns 0 for all files
    - grep -rc "Ptr(.*," src/templates/ returns 0 for all files (no old two-arg constructor)
    - grep -c "fail_ptr_cast" on the only .cast() match in test/fixtures/ returns 1
    - docs/09-memory.md contains "const p: Ptr(i32) = &x" (new syntax)
    - docs/09-memory.md contains "auto-borrow" (const auto-borrow documented)
    - ./testall.sh exits 0
  </acceptance_criteria>
  <done>All fixtures use new syntax, docs are current, test suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Tamga build and test suite</name>
  <files>None</files>
  <action>User verifies Tamga builds and compiler test suite passes after the compatibility updates.</action>
  <what-built>Updated Tamga companion project to new Ptr syntax and verified all compiler fixtures, example module, and language docs are current after the Phase 8-9 breaking changes.</what-built>
  <how-to-verify>
    1. Check Tamga edit: open /home/yunus/Projects/orhon/tamga/src/example/data_types.orh line 85 — should read `const p: Ptr(i32) = &x`
    2. Confirm Tamga builds: `cd /home/yunus/Projects/orhon/tamga && orhon build` succeeds
    3. Confirm compiler tests: `cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh` passes all 11 stages
  </how-to-verify>
  <verify>User confirms all three checks pass</verify>
  <done>User approves Tamga build and test suite results</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Tamga compiles with cleared cache under new Ptr syntax
- No stale .cast() or Ptr(T, &x) syntax in example module or tester fixtures
- docs/09-memory.md documents both auto-borrow and new Ptr syntax
- ./testall.sh passes all 11 stages
</verification>

<success_criteria>
COMP-01: Tamga data_types.orh updated from `Ptr(i32, &x)` to `&x`, clean rebuild succeeds
COMP-02: Example module and tester verified — no old syntax remains (except negative test)
COMP-03: docs/09-memory.md and docs/05-functions.md verified current
</success_criteria>

<output>
After completion, create `.planning/phases/10-compatibility-updates/10-01-SUMMARY.md`
</output>
