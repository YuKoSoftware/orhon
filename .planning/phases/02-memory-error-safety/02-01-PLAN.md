---
phase: 02-memory-error-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codegen.zig
  - test/08_codegen.sh
autonomous: true
requirements:
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - "String interpolation @{expr} in generated Zig propagates OOM instead of panicking"
    - "No compiler-internal catch unreachable remains on allocPrint calls in codegen"
  artifacts:
    - path: "src/codegen.zig"
      provides: "Fixed interpolation error propagation"
      contains: "catch |err| return err"
    - path: "test/08_codegen.sh"
      provides: "Codegen quality check for interpolation safety"
      contains: "catch |err| return err"
  key_links:
    - from: "src/codegen.zig"
      to: "generated .zig files"
      via: "emit() calls in generateInterpolatedString and generateInterpolatedStringMir"
      pattern: 'catch \|err\| return err'
---

<objective>
Fix the two compiler-internal OOM crash sites in codegen where string interpolation
emits `catch unreachable` into generated Zig. Add a codegen quality test to prevent
regression.

Purpose: MEM-01 and MEM-02 — interpolation temp buffers must propagate OOM, not panic.
The MIR lowerer already injects `defer page_allocator.free(...)` for cleanup (MEM-01 strategy
is already implemented). The only gap is the emitted `catch unreachable` at the allocPrint
call site, which panics on OOM instead of propagating the error.

Output: `codegen.zig` with safe error propagation at both interpolation sites, plus a
regression test in `08_codegen.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-error-safety/02-RESEARCH.md

<interfaces>
From src/codegen.zig (relevant functions):

Line 2542: generateInterpolatedString — AST path
  - Emits: `std.fmt.allocPrint(std.heap.page_allocator, "...", .{args}) catch unreachable`
  - Line 2584: `try self.emit("}) catch unreachable");`

Line ~2960: generateInterpolatedStringMir — MIR path
  - Same pattern as AST path
  - Line 2983: `try self.emit("}) catch unreachable");`

Both functions end by emitting `catch unreachable` — this is the ONLY change needed.
The `self.emit()` method returns `anyerror!void`.

IMPORTANT: Do NOT touch any other `catch unreachable` in codegen.zig:
  - Lines 700, 726, 950, 983 — thread handle codegen (intentional in generated Zig)
  - Lines 1838, 1855, 1861, 2257, 2270, 2277 — error union .value unwrap (semantically correct)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix interpolation catch unreachable in codegen.zig</name>
  <files>src/codegen.zig</files>
  <read_first>
    - src/codegen.zig (lines 2575-2590 and 2970-2990 — the two interpolation functions)
  </read_first>
  <behavior>
    - Generated Zig for `@{expr}` interpolation contains `catch |err| return err` instead of `catch unreachable`
    - All other `catch unreachable` in codegen.zig remain unchanged (thread handles, .value unwraps)
    - `zig build test` passes (no unit test regressions)
  </behavior>
  <action>
    In `src/codegen.zig`, make exactly two changes:

    1. Line 2584 — in `generateInterpolatedString` (AST path):
       Change: `try self.emit("}) catch unreachable");`
       To:     `try self.emit("}) catch |err| return err");`

    2. Line 2983 — in `generateInterpolatedStringMir` (MIR path):
       Change: `try self.emit("}) catch unreachable");`
       To:     `try self.emit("}) catch |err| return err");`

    Do NOT modify any other lines. The other 11 `catch unreachable` occurrences in
    codegen.zig are intentional — they emit semantically correct Zig for thread handles
    and error union unwraps.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'catch unreachable' src/codegen.zig | wc -l` returns 11 (was 13, two removed)
    - `grep -n 'catch |err| return err' src/codegen.zig` shows exactly 2 matches (lines ~2584 and ~2983)
    - `zig build test` exits 0
  </acceptance_criteria>
  <done>Both interpolation codegen functions emit `catch |err| return err` instead of `catch unreachable`</done>
</task>

<task type="auto">
  <name>Task 2: Add codegen regression test for interpolation safety</name>
  <files>test/08_codegen.sh</files>
  <read_first>
    - test/08_codegen.sh (full file — understand existing test patterns)
    - test/helpers.sh (understand pass/fail helper functions)
  </read_first>
  <action>
    In `test/08_codegen.sh`, before the final `report_results` line, add a new test section
    that creates a minimal project using string interpolation and verifies the generated
    Zig does NOT contain `catch unreachable` for the interpolation allocPrint call.

    Add this test block:

    ```bash
    # ── Interpolation error propagation ─────────────────────────────

    cd "$TESTDIR"
    mkdir -p interptest/src
    cat > interptest/src/main.orh <<'ORHON'
    module main
    #name    = "interptest"
    #version = Version(1, 0, 0)
    #build   = exe
    import std::console
    func main() void {
        const name: str = "world"
        console.println("hello @{name}")
    }
    ORHON
    cd interptest
    "$ORHON" build >/dev/null 2>&1 || true

    if grep -q 'catch |err| return err' .orh-cache/generated/main.zig; then
        pass "interpolation propagates OOM (no catch unreachable)"
    else
        fail "interpolation propagates OOM (no catch unreachable)"
    fi
    ```

    Place this block after the "bare call discard" section and before `report_results`.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && bash test/08_codegen.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'interpolation propagates OOM' test/08_codegen.sh` returns 2 (pass and fail lines)
    - `bash test/08_codegen.sh` exits 0
    - Output includes "PASS interpolation propagates OOM"
  </acceptance_criteria>
  <done>Codegen test suite includes a check that interpolation uses safe error propagation, test passes</done>
</task>

</tasks>

<verification>
- `zig build test` passes (unit tests)
- `bash test/08_codegen.sh` passes (codegen quality checks including new interpolation test)
- `grep -c 'catch unreachable' src/codegen.zig` returns 11 (down from 13)
- `grep -c 'catch |err| return err' src/codegen.zig` returns 2
</verification>

<success_criteria>
- Generated Zig for string interpolation uses `catch |err| return err` not `catch unreachable`
- Regression test verifies this in the codegen test suite
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-error-safety/02-01-SUMMARY.md`
</output>
