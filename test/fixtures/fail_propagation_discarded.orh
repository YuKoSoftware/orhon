module fixture

#version = (1, 0, 0)
#build   = exe

func may_fail(n: i32) (Error | i32) {
    if(n == 0) {
        return Error("bad")
    }
    return n
}

// bare call — result discarded, union never handled
func bad() void {
    may_fail(1)
}

func main() void { }
