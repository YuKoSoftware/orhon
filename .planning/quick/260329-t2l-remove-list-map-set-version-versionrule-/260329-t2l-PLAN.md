---
phase: quick
plan: 260329-t2l
type: execute
wave: 1
depends_on: []
files_modified:
  - src/builtins.zig
autonomous: true
---

<objective>
Remove dead and misplaced types from BUILTIN_TYPES in builtins.zig.

Purpose: List, Map, Set belong to std::collections (bridge types, not compiler intrinsics). Version, VersionRule, Dependency are dead code with zero references outside builtins.zig. Only Ptr, RawPtr, VolatilePtr, Handle, Error, Vector are true compiler-level builtin types.

Output: Cleaned BUILTIN_TYPES array, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/builtins.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim BUILTIN_TYPES and update tests</name>
  <files>src/builtins.zig</files>
  <action>
1. Replace the BUILTIN_TYPES array (lines 8-21) with only these 6 entries:
   "Ptr", "RawPtr", "VolatilePtr", "Handle", "Error", "Vector"

2. In test "builtin type detection" (line 124):
   - Remove these 3 assertions:
     `try std.testing.expect(isBuiltinType("List"));`
     `try std.testing.expect(isBuiltinType("Map"));`
     `try std.testing.expect(isBuiltinType("Set"));`
   - Add negative assertions to confirm they are no longer builtins:
     `try std.testing.expect(!isBuiltinType("List"));`
     `try std.testing.expect(!isBuiltinType("Map"));`
     `try std.testing.expect(!isBuiltinType("Set"));`
     `try std.testing.expect(!isBuiltinType("Version"));`

3. No changes needed to isValueType, isCompilerFunc, isBuiltinValue, primitiveToZig, or any other file.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <done>BUILTIN_TYPES contains exactly 6 entries (Ptr, RawPtr, VolatilePtr, Handle, Error, Vector). All unit tests pass. List/Map/Set/Version/VersionRule/Dependency removed.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite validation</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions. The removal should be safe because:
- VersionRule and Dependency have zero references outside builtins.zig
- List, Map, Set are resolved through std::collections bridge imports, not builtins
- Version is only used in build metadata context (BUILD_FIELDS), not as a builtin type

Run: ./testall.sh

If any test fails, investigate whether it relies on isBuiltinType() returning true for the removed types and fix accordingly.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 test stages pass with zero failures.</done>
</task>

</tasks>

<verification>
- `zig build test` passes (unit tests including updated builtins tests)
- `./testall.sh` passes all 11 stages
- `grep -c '"' src/builtins.zig | head -1` confirms BUILTIN_TYPES has exactly 6 string entries
</verification>

<success_criteria>
BUILTIN_TYPES reduced from 12 to 6 entries. No test regressions. Dead code (VersionRule, Dependency) and bridge types (List, Map, Set) and misplaced type (Version) removed from compiler intrinsics.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-t2l-remove-list-map-set-version-versionrule-/260329-t2l-SUMMARY.md`
</output>
