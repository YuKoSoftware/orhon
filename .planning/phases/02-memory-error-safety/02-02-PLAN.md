---
phase: 02-memory-error-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/std/console.zig
  - src/std/tui.zig
  - src/std/stream.zig
  - src/std/collections.zig
  - src/std/fs.zig
  - src/std/str.zig
  - src/std/json.zig
  - src/std/csv.zig
  - src/std/yaml.zig
  - src/std/toml.zig
  - src/std/regex.zig
  - src/std/ini.zig
  - src/std/xml.zig
  - src/std/http.zig
  - src/std/system.zig
autonomous: true
requirements:
  - MEM-03

must_haves:
  truths:
    - "No stdlib bridge file silently truncates data on allocation failure"
    - "Fire-and-forget I/O catch {} sites are documented with a comment"
    - "Data-builder functions propagate or gracefully degrade on OOM"
  artifacts:
    - path: "src/std/console.zig"
      provides: "Documented fire-and-forget I/O pattern"
      contains: "fire-and-forget"
    - path: "src/std/collections.zig"
      provides: "Documented best-effort OOM policy"
      contains: "best-effort"
    - path: "src/std/json.zig"
      provides: "OOM-safe data builders"
    - path: "src/std/csv.zig"
      provides: "OOM-safe data builders"
  key_links:
    - from: "src/std/*.zig"
      to: "generated Zig programs"
      via: "@embedFile in main.zig, extracted to .orh-cache/std/"
      pattern: "catch"
---

<objective>
Eliminate silent error suppression across all 15 stdlib bridge files by classifying
each `catch {}` as either fire-and-forget I/O (document with comment) or data-builder
OOM (fix with propagation).

Purpose: MEM-03 — 103 `catch {}` instances silently suppress allocation and I/O failures.
A "safe" language compiler cannot silently discard errors in its own stdlib.

Output: All 15 stdlib `.zig` files with documented I/O catch sites and fixed data-builder
catch sites. Zero silent data truncation on OOM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-error-safety/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Category A (fire-and-forget I/O) and fix Category B in first 8 files</name>
  <files>
    src/std/console.zig
    src/std/tui.zig
    src/std/stream.zig
    src/std/collections.zig
    src/std/fs.zig
    src/std/str.zig
    src/std/json.zig
    src/std/csv.zig
  </files>
  <read_first>
    - src/std/console.zig (6 catch {} — all Category A fire-and-forget)
    - src/std/tui.zig (9 catch {} — all Category A fire-and-forget)
    - src/std/stream.zig (2 catch {} — Category A)
    - src/std/collections.zig (6 catch {} — intentional best-effort, document policy)
    - src/std/fs.zig (3 catch {} — mixed: seekTo is best-effort, cleanup deleteDir is best-effort)
    - src/std/str.zig (2 catch {} — in splitBy, Category B)
    - src/std/json.zig (11 catch {} — Category B data builders)
    - src/std/csv.zig (12 catch {} — Category B data builders)
  </read_first>
  <action>
    Process each file according to its category:

    **Category A — Fire-and-Forget I/O (add comment, keep catch {}):**

    `console.zig` (6 sites): Add comment on first `catch {}` in each function:
    `// fire-and-forget: I/O in void fn`
    Keep all `catch {}` as-is. These are `print`, `println`, `flush`, `debugPrint`.

    `tui.zig` (9 sites): Add comment on first `catch {}` in each render/write block:
    `// fire-and-forget: terminal I/O in void fn`
    Keep all `catch {}` as-is. These are cursor movement, style writes, render calls.

    `stream.zig` (2 sites): Add comment: `// fire-and-forget: stream I/O in void fn`

    `collections.zig` (6 sites): Add block comment at top of file (after the file header comment):
    ```
    // OOM policy: collection methods are best-effort — OOM silently drops items.
    // Use allocator-aware Zig collections directly for hard guarantees.
    ```
    Keep all `catch {}` as-is in `add()`, `put()`, `insert()` etc.

    `fs.zig` (3 sites): Add comment: `// best-effort: seek/cleanup failure is non-fatal`

    **Category B — Data Builders (fix silent truncation):**

    `str.zig` (2 sites in `splitBy`): Read the function. For append inside loops,
    change `catch {}` to `catch continue` (skip the split segment on OOM, keep processing).

    `json.zig` (11 sites): Read each function's return type:
    - Functions returning `[]const u8`: change `catch {}` to `catch return "{}"`
      for object builders, `catch return "[]"` for array builders, `catch return ""`
      for generic string builders.
    - Append calls in loops: change `catch {}` to `catch continue`.
    - Read each function carefully — match the fallback value to the expected return shape.

    `csv.zig` (12 sites): Read each function's return type:
    - Functions returning `[][]const u8` or `[][][]const u8`: change `catch {}` to
      `catch return &.{}` for top-level returns, `catch continue` for loop appends.
    - Read each function carefully before choosing fallback.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fire-and-forget' src/std/console.zig` returns at least 1
    - `grep -c 'fire-and-forget' src/std/tui.zig` returns at least 1
    - `grep -c 'best-effort' src/std/collections.zig` returns at least 1
    - `grep -c 'catch {}' src/std/json.zig` returns 0
    - `grep -c 'catch {}' src/std/csv.zig` returns 0
    - `grep -c 'catch {}' src/std/str.zig` returns 0
    - `zig build test` exits 0
  </acceptance_criteria>
  <done>First 8 stdlib files have documented I/O catch sites and fixed data-builder catch sites</done>
</task>

<task type="auto">
  <name>Task 2: Fix Category B in remaining 7 files and run full test suite</name>
  <files>
    src/std/yaml.zig
    src/std/toml.zig
    src/std/regex.zig
    src/std/ini.zig
    src/std/xml.zig
    src/std/http.zig
    src/std/system.zig
  </files>
  <read_first>
    - src/std/yaml.zig (14 catch {} — Category B, mostly loop appends)
    - src/std/toml.zig (8 catch {} — Category B, mixed returns)
    - src/std/regex.zig (7 catch {} — Category B, string builders returning []const u8)
    - src/std/ini.zig (7 catch {} — Category B, string builders returning []const u8)
    - src/std/xml.zig (6 catch {} — Category B, string builders returning []const u8)
    - src/std/http.zig (3 catch {} — Category B, URL builders returning []const u8)
    - src/std/system.zig (7 catch {} — mixed: signal handlers are Category A, string builders are Category B)
  </read_first>
  <action>
    Process each file:

    `yaml.zig` (14 sites): Read each function's return type.
    - Loop appends building lists: change `catch {}` to `catch continue`
    - String builder returns: change `catch {}` to `catch return ""`

    `toml.zig` (8 sites): Read each function's return type.
    - `[]const u8` returns: `catch return ""`
    - Loop appends: `catch continue`
    - Void returns: add comment `// fire-and-forget` and keep `catch {}`

    `regex.zig` (7 sites): All return `[]const u8`.
    - Change `catch {}` to `catch return ""` for string result builders.
    - Loop appends: `catch continue`.

    `ini.zig` (7 sites): All return `[]const u8`.
    - Change `catch {}` to `catch return ""` for string builders.
    - Loop appends: `catch continue`.

    `xml.zig` (6 sites): All return `[]const u8`.
    - Change `catch {}` to `catch return ""` for string builders.

    `http.zig` (3 sites): URL builders returning `[]const u8`.
    - Change `catch {}` to `catch return ""`.

    `system.zig` (7 sites): Mixed.
    - Signal handlers (void fn): add comment `// fire-and-forget: signal handler`, keep `catch {}`.
    - String builder returns: change `catch {}` to `catch return ""`.
    - Read each function to determine which category.

    IMPORTANT: For every file, read the function containing the `catch {}` to verify
    the return type before choosing the fallback. Do NOT blindly apply `catch return ""`
    to functions that return slices-of-slices, structs, or void.

    After all changes, run `./testall.sh` to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'catch {}' src/std/yaml.zig` returns 0
    - `grep -c 'catch {}' src/std/toml.zig` returns 0 (unless void fns remain documented)
    - `grep -c 'catch {}' src/std/regex.zig` returns 0
    - `grep -c 'catch {}' src/std/ini.zig` returns 0
    - `grep -c 'catch {}' src/std/xml.zig` returns 0
    - `grep -c 'catch {}' src/std/http.zig` returns 0
    - `grep 'catch {}' src/std/system.zig` only shows lines with a `// fire-and-forget` comment
    - `./testall.sh` exits 0 (all 11 test stages pass)
  </acceptance_criteria>
  <done>All 103 catch {} sites across 15 stdlib files are either fixed (Category B) or documented (Category A). Full test suite passes.</done>
</task>

</tasks>

<verification>
- `./testall.sh` passes all 11 stages
- Every remaining `catch {}` in `src/std/*.zig` has an associated comment explaining why
- No data-builder function silently truncates output on OOM
</verification>

<success_criteria>
- Zero uncommented `catch {}` in Category B (data-builder) functions
- All Category A (fire-and-forget I/O) sites documented with comments
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-error-safety/02-02-SUMMARY.md`
</output>
