---
phase: 28-cross-compile-cache-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codegen.zig
  - docs/TODO.md
autonomous: true
requirements:
  - CLN-01
  - DOC-01

must_haves:
  truths:
    - "Async(T) does not exist anywhere in the compiler source"
    - "TODO.md accurately reflects all bugs fixed through v0.16"
  artifacts:
    - path: "src/codegen.zig"
      provides: "Async(T) branch removed from typeToZig"
    - path: "docs/TODO.md"
      provides: "Updated bug status reflecting v0.16 fixes"
  key_links:
    - from: "src/codegen.zig"
      to: "typeToZig function"
      via: "type_generic branch"
      pattern: "type_generic.*blk"
---

<objective>
Remove dead Async(T) code from codegen and update TODO.md to reflect all v0.16 bug fixes.

Purpose: No dead language constructs in the compiler, documentation reflects reality.
Output: Clean codegen.zig without Async references, accurate TODO.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/codegen.zig
@docs/TODO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Async(T) from codegen and update TODO.md</name>
  <files>src/codegen.zig, docs/TODO.md</files>
  <read_first>src/codegen.zig (lines 4176-4184 — the Async branch in typeToZig), docs/TODO.md (full file)</read_first>
  <action>
**CLN-01 — Remove Async(T) from codegen.zig:**

In `typeToZig()` around line 4179, there is an `else if` branch that checks for `Async` generic name and reports an error. Remove the entire `else if` block (lines 4179-4183):
```zig
} else if (std.mem.eql(u8, g.name, "Async")) {
    const err_msg = try std.fmt.allocPrint(self.allocator, "Async(T) is not yet implemented — cannot use as a type", .{});
    defer self.allocator.free(err_msg);
    try self.reporter.report(.{ .message = err_msg });
    break :blk "void"; // fallback after error
```

The `Thread` branch before it and the `Handle` branch after it should connect directly. Verify no other references to "Async" exist in `src/` (already confirmed: lexer, PEG grammar, and builder have no Async references).

**DOC-01 — Update TODO.md:**

1. Mark the open sidecar bug as fixed (line 23-26): "Build — multi-file module with Zig sidecar" was fixed in Phase 27 (BLD-01). Apply strikethrough and add fix note:
   `### ~~Build — multi-file module with Zig sidecar: "file exists in two modules"~~ — fixed v0.16 Phase 27`
   Add description: Fixed: sidecar deduplication prevents duplicate module registration.

2. Verify the 4 bugs from the requirement are already marked fixed:
   - cast_to_enum (line 15-17) — should have strikethrough
   - null_multi_union (line 10-13) — should have strikethrough
   - empty_struct (line 19-21) — should have strikethrough
   - size keyword (line 28-30) — should have strikethrough

3. Add entries to the Done section at the bottom for v0.16 work:
   - "Bridge codegen fixes (const &, error-union params, pub export fn)" — Done in v0.16 Phase 25
   - "Cross-module is operator, negative literal parsing" — Done in v0.16 Phase 26
   - "C interop: sidecar dedup, cimport include paths, linkSystemLibrary" — Done in v0.16 Phase 27
   - "Cross-compilation target fix, build cache cleanup" — Done in v0.16 Phase 28

4. Remove the Async entry from Features section (line 364-369 "async keyword — IO concurrency") — NO, keep this. The async KEYWORD design discussion is a future feature. Only the dead `Async(T)` codegen branch is removed. The TODO entry about async as a future language feature should stay.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && grep -c "Async" src/codegen.zig && zig build test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "Async" src/codegen.zig returns 0
- grep "~~Build.*sidecar.*fixed" docs/TODO.md confirms sidecar bug marked as fixed
- grep "v0.16 Phase 25" docs/TODO.md confirms bridge codegen fixes in Done section
- grep "v0.16 Phase 27" docs/TODO.md confirms C interop fixes in Done section
  </acceptance_criteria>
  <done>
- Async(T) branch completely removed from codegen.zig typeToZig function
- No "Async" string anywhere in src/codegen.zig
- TODO.md sidecar bug (line 23) marked as fixed with strikethrough
- TODO.md Done section includes all v0.16 phase accomplishments
- zig build test passes
  </done>
</task>

</tasks>

<verification>
- No Async references in codegen.zig
- TODO.md accurately reflects all fixed bugs and v0.16 accomplishments
- All tests pass
</verification>

<success_criteria>
- CLN-01: Async(T) completely removed from grammar and codegen — zero references
- DOC-01: TODO.md marks all v0.16 fixed bugs with strikethrough, Done section updated
- zig build test passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/28-cross-compile-cache-docs/28-02-SUMMARY.md`
</output>
