---
phase: 24-cimport-unification
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - docs/14-zig-bridge.md
  - docs/11-modules.md
  - src/templates/example/example.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh
autonomous: false
requirements: [CIMP-05, CIMP-06]

must_haves:
  truths:
    - "Tamga framework's three bridge modules use #cimport syntax with zero legacy directives"
    - "tamga_vk3d does NOT re-declare SDL3 — it gets SDL types via import tamga_sdl3"
    - "tamga_vma uses source: for the C++ impl file and does NOT linkSystemLibrary"
    - "docs/14-zig-bridge.md documents #cimport with block syntax examples"
    - "docs/11-modules.md references #cimport instead of #linkC"
    - "Example module documents #cimport syntax in its bridge section"
  artifacts:
    - path: "docs/14-zig-bridge.md"
      provides: "Updated C interop documentation with #cimport"
      contains: "#cimport"
    - path: "docs/11-modules.md"
      provides: "Updated metadata directive list"
      contains: "#cimport"
    - path: "src/templates/example/example.orh"
      provides: "Example module with #cimport documentation section"
      contains: "#cimport"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh"
      provides: "Migrated SDL3 bridge"
      contains: "#cimport"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh"
      provides: "Migrated Vulkan bridge"
      contains: "#cimport"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh"
      provides: "Migrated VMA bridge with source:"
      contains: "#cimport"
  key_links:
    - from: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh"
      to: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh"
      via: "import tamga_sdl3 for transitive SDL types (D-09/D-10)"
      pattern: "import tamga_sdl3"
    - from: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh"
      to: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh"
      via: "import tamga_vk3d for transitive Vulkan types"
      pattern: "import tamga_vk3d"
---

<objective>
Migrate the Tamga framework to #cimport syntax, update the example module and all documentation.

Purpose: Complete the migration so no legacy directives remain in any project, and docs accurately reflect the new unified syntax. The example module documents #cimport for new users per CLAUDE.md mandate.
Output: Tamga builds with #cimport, example module updated, docs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cimport-unification/24-CONTEXT.md
@.planning/phases/24-cimport-unification/24-01-SUMMARY.md

@docs/14-zig-bridge.md
@docs/11-modules.md
@src/templates/example/example.orh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Tamga bridge modules to #cimport syntax</name>
  <read_first>
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh (full file — see current #linkC usage)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh (full file — see current #linkC + #cInclude + duplicate #linkC "SDL3")
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh (full file — see current #linkC + #cInclude + #csource + #linkCpp)
    - .planning/phases/24-cimport-unification/24-CONTEXT.md (specifics section — exact migration map)
  </read_first>
  <files>
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh,
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh,
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh
  </files>
  <action>
    **tamga_sdl3.orh:** Replace `#linkC "SDL3"` with:
    ```
    #cimport "SDL3" { include: "SDL3/SDL.h" }
    ```
    Remove any other C-interop metadata lines. Keep all other metadata (#name, #version, #build) and code unchanged.

    **tamga_vk3d.orh:** Replace ALL of these lines:
    ```
    #linkC "vulkan"
    #linkC "SDL3"
    #cInclude "vulkan/vulkan.h"
    ```
    With just:
    ```
    #cimport "vulkan" { include: "vulkan/vulkan.h" }
    ```
    The `#linkC "SDL3"` line is REMOVED entirely — per D-09, tamga_vk3d gets SDL types transitively via `import tamga_sdl3` which already exists in the file. Do NOT add `#cimport "SDL3"` here — that would trigger the duplicate detection error (CIMP-03).

    **tamga_vma.orh:** Replace ALL of these lines:
    ```
    #linkC "vulkan"
    #cInclude "vulkan/vulkan.h"
    #csource "../../src/TamgaVMA/vma_impl.cpp"
    #linkCpp
    ```
    With just:
    ```
    #cimport "vma" { include: "vk_mem_alloc.h", source: "../../src/TamgaVMA/vma_impl.cpp" }
    ```
    The `#linkC "vulkan"` and `#cInclude "vulkan/vulkan.h"` lines are REMOVED — tamga_vma gets Vulkan types via `import tamga_vk3d`. C++ linking is auto-detected from the `.cpp` extension on source:. The lib name is `"vma"` (not `"vulkan"`) — it is source-only so `linkSystemLibrary` will be skipped.
  </action>
  <verify>
    <automated>grep -c "#linkC\|#cInclude\|#csource\|#linkCpp" /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>
    All three Tamga bridge modules use #cimport with zero legacy directives. Cross-module type sharing
    via import is preserved. Source-only VMA bridge uses name-for-identity pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update example module, docs, and run full test suite</name>
  <read_first>
    - src/templates/example/example.orh (full file — find "Bridge Declarations" section at ~line 42)
    - docs/14-zig-bridge.md (full file — find "Calling C Through Zig" section at ~line 182 and "Multiple #linkC" at ~line 212)
    - docs/11-modules.md (line 25 — metadata directive list mention)
  </read_first>
  <files>src/templates/example/example.orh, docs/14-zig-bridge.md, docs/11-modules.md</files>
  <action>
    **src/templates/example/example.orh — add #cimport section (CIMP-06):**

    After the existing "Bridge Declarations" comment section (~line 51, after the line about
    ".orh file with a same-name .zig sidecar"), add a new section documenting `#cimport`:

    ```
    // --- C Library Import (#cimport) -------------------------------------------

    // bridge modules that call C code use #cimport to declare the C library dependency.
    // The block must include an include: key for the C header. source: is optional.
    //
    //   #cimport "SDL3" { include: "SDL3/SDL.h" }
    //   #cimport "vma" { include: "vk_mem_alloc.h", source: "vma_impl.cpp" }
    //
    // Each C library can only be declared once across the entire project.
    // Other modules access C types by importing the owning bridge module.
    // When source: is present, the library is compiled from source (no system link).
    // C++ linking is auto-detected from .cpp/.cc/.cxx file extensions.
    ```

    This is comment-only documentation (like the existing bridge section) since the example
    module cannot actually use #cimport without real C headers. The example module must remain
    compilable per CLAUDE.md rules. No old directives exist in the example module that need
    removal — only the new documentation section is needed.

    **docs/14-zig-bridge.md — "Calling C Through Zig" section (lines 182-213):**

    Replace the entire section with updated content:

    ```markdown
    ## Calling C Through Zig

    C interop goes through `.zig` bridge files. The `.orh` file exposes a clean Orhon API,
    the `.zig` file handles all C details internally.

    Use `#cimport` in the anchor file to declare C library dependencies. The block must
    always include an `include:` key specifying the C header. The compiler generates the
    correct `linkSystemLibrary` + `linkLibC` calls and a shared `@cImport` wrapper module
    in the build system.

    ```
    // sdl.orh — clean Orhon interface
    module sdl
    #cimport "SDL3" { include: "SDL3/SDL.h" }

    pub bridge func init() void
    pub bridge func quit() void
    ```

    ```zig
    // sdl.zig — Zig handles all C interop
    // The compiler generates a shared @cImport module; import it:
    const c = @import("SDL_c").c;

    pub fn init() void {
        _ = c.SDL_Init(c.SDL_INIT_VIDEO);
    }

    pub fn quit() void {
        c.SDL_Quit();
    }
    ```

    ### Block Syntax

    The `#cimport` block uses colon-suffix keys with comma separators:

    - `include:` — C header path (required)
    - `source:` — C/C++ source file to compile (optional)

    Unknown keys produce a compile error.

    ```
    // Source-only library (no system lib linked)
    #cimport "vma" { include: "vk_mem_alloc.h", source: "../../src/vma_impl.cpp" }
    ```

    When `source:` is present, the compiler skips `linkSystemLibrary` — the library name
    is used only for project-wide identity and deduplication. C++ linking is auto-detected
    from `.cpp`, `.cc`, or `.cxx` file extensions.

    ### One Library Per Project

    Each C library can only be declared with `#cimport` once across the entire project.
    Other modules access the library's C types by importing the owning bridge module:

    ```
    // tamga_vk3d.orh — gets SDL types via import, not re-declaring #cimport
    module tamga_vk3d
    import tamga_sdl3
    #cimport "vulkan" { include: "vulkan/vulkan.h" }
    ```

    Declaring `#cimport "SDL3"` in both `tamga_sdl3` and `tamga_vk3d` is a compile error.
    ```

    Remove the old line about "Multiple `#linkC` directives are allowed" (line 212).

    **docs/11-modules.md — metadata directive list (line 25):**

    Replace:
    ```
    (`#build`, `#name`, `#version`, `#dep`, `#linkC`, etc.).
    ```
    With:
    ```
    (`#build`, `#name`, `#version`, `#dep`, `#cimport`, etc.).
    ```

    **Full test suite:**
    Run `./testall.sh` to verify the entire compiler still works after all Phase 24 changes.
    The example module is compiled as part of test stage 09 — this confirms the new comment
    section does not break the example module.
  </action>
  <verify>
    <automated>grep -c "#cimport" src/templates/example/example.orh && grep -c "#cimport" docs/14-zig-bridge.md && grep -c "#linkC" docs/14-zig-bridge.md; grep "#cimport" docs/11-modules.md; ./testall.sh 2>&1 | tail -20</automated>
  </verify>
  <done>
    Example module documents #cimport syntax in its bridge section (comment-only, compilable).
    Documentation reflects #cimport syntax with block form, source-only pattern, and one-per-project rule.
    Full test suite passes confirming no regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete #cimport implementation</name>
  <files>none</files>
  <action>
    Human reviews the complete #cimport implementation.
    What was built: compiler pipeline (grammar, builder, declarations, main.zig, zig_runner),
    example module documentation, spec docs, and Tamga migration.
  </action>
  <verify>
    1. Run `./testall.sh` — all 11 stages should pass
    2. Verify no old directives remain: `grep -r "#linkC\|#cInclude\|#csource\|#linkCpp" src/ docs/`
    3. Review Tamga .orh files for correct #cimport syntax
    4. Review example module #cimport section in src/templates/example/example.orh
    5. Optionally build Tamga if SDL3/Vulkan installed
    Type "approved" or describe issues to continue.
  </verify>
  <done>Human confirms: test suite green, Tamga migrated correctly, docs accurate, no legacy directives remain.</done>
</task>

</tasks>

<verification>
1. `./testall.sh` — full test suite green
2. `grep -r "#linkC\|#cInclude\|#csource\|#linkCpp" src/ docs/` — returns nothing
3. All three Tamga .orh files contain #cimport, zero legacy directives
4. docs/14-zig-bridge.md documents the new syntax
5. src/templates/example/example.orh contains #cimport documentation section
</verification>

<success_criteria>
- Tamga's three bridge modules compile with #cimport (no legacy directives)
- tamga_vk3d uses import for SDL types, not re-declared #cimport
- tamga_vma source-only pattern works (source: present, no linkSystemLibrary)
- Example module documents #cimport syntax (comment section, compilable)
- docs accurately describe #cimport block syntax, one-per-project rule, and source-only pattern
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-cimport-unification/24-02-SUMMARY.md`
</output>
