---
phase: 02-memory-error-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mir.zig
  - test/fixtures/tester.orh
  - src/templates/example/data_types.orh
autonomous: true
requirements:
  - MEM-04

must_haves:
  truths:
    - "Ptr(T).cast(&x) and RawPtr(T).cast(&x) compile and produce correct pointer values"
    - "Tester module uses .cast() method-style constructors for all Ptr/RawPtr creation"
    - "Example module uses .cast() method-style constructors for Ptr creation"
  artifacts:
    - path: "src/mir.zig"
      provides: "MIR annotator recognition of Type(T).cast(addr) as ptr_expr"
    - path: "test/fixtures/tester.orh"
      provides: "Migrated Ptr/RawPtr constructors using .cast()"
      contains: ".cast("
    - path: "src/templates/example/data_types.orh"
      provides: "Migrated Ptr constructor using .cast()"
      contains: ".cast("
  key_links:
    - from: "src/mir.zig"
      to: "src/codegen.zig"
      via: "MirNode with kind .ptr_expr consumed by generatePtrExprMir"
      pattern: "ptr_expr"
    - from: "test/fixtures/tester.orh"
      to: "test/10_runtime.sh"
      via: "tester binary compiled and output checked"
      pattern: "cast"
---

<objective>
Add `.cast()` method-style constructor support for `Ptr(T)` and `RawPtr(T)`, then
migrate tester.orh and the example module to use the new syntax.

Purpose: MEM-04 — standardize pointer constructors to method-style `.cast()` consistent
with the `.new()` pattern already used for collections. No bare type-as-value construction
should remain.

Output: MIR annotator recognizes `Ptr(T).cast(&x)` as a ptr_expr, tester.orh and
example module migrated, all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-error-safety/02-RESEARCH.md

<interfaces>
From src/mir.zig — MirAnnotator ptr_expr creation:
  The MirAnnotator creates `ptr_expr` MirNodes when it encounters Ptr/RawPtr
  constructor patterns. The codegen functions `generatePtrExpr` (AST path, ~line 3400)
  and `generatePtrExprMir` (MIR path, ~line 2993) consume these nodes.

  Key: `Ptr(T).cast(&x)` will parse as a method_call on a generic type expression,
  NOT as a ptr_expr AST node. The MIR annotator must intercept the method_call
  pattern and convert it into a ptr_expr MirNode so codegen handles it correctly.

From test/fixtures/tester.orh — current Ptr usage:
  Line 692: `const raw: RawPtr(i32) = RawPtr(i32, &x)`
  Line 699: `const raw: RawPtr(i32) = RawPtr(i32, &arr)`
  Line 705: `const p: Ptr(i32) = Ptr(i32, &x)`

From src/templates/example/data_types.orh:
  Line 85: `const p: Ptr(i32) = Ptr(i32, &x)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .cast() method-style constructor recognition in MIR annotator</name>
  <files>src/mir.zig</files>
  <read_first>
    - src/mir.zig (search for `ptr_expr` to find where MirAnnotator creates ptr_expr nodes — understand the pattern)
    - src/mir.zig (search for `method_call` or `func_call` to understand how method calls on types are currently handled)
    - src/codegen.zig (search for `generatePtrExpr` to understand what a ptr_expr MirNode needs to contain — name field = "Ptr" or "RawPtr", captures contain the address argument)
    - src/parser.zig (search for `method_call` node kind to understand the AST structure)
  </read_first>
  <action>
    **Step 1: Investigate AST structure.**
    First, understand how `Ptr(i32).cast(&x)` parses. Create a minimal test fixture:
    ```
    mkdir -p /tmp/ptrtest/src
    ```
    Write a minimal `.orh` file with `Ptr(i32).cast(&x)` and run `orhon build` in debug
    mode or use the parser to see what AST node is produced. The call `Type(T).cast(arg)`
    likely parses as a `method_call` node where the receiver is a generic type expression.

    **Step 2: Add recognition in MIR annotator.**
    In `src/mir.zig`, find where the `MirAnnotator` processes `method_call` or `func_call`
    nodes. Add a check: if the method name is `"cast"` and the receiver is a generic type
    expression whose base name is `"Ptr"` or `"RawPtr"`, convert this into a `ptr_expr`
    MirNode with:
    - `name` = `"Ptr"` or `"RawPtr"` (from the receiver type name)
    - captures containing the address argument (the argument to `.cast()`)

    This makes the existing codegen `generatePtrExprMir` handle it without any codegen changes.

    **Step 3: Keep backward compatibility.**
    The old syntax `Ptr(T, &x)` must continue to work — do not remove the existing
    ptr_expr recognition. The new `.cast()` path is an additional recognition pattern.

    **Step 4: Verify with a minimal fixture.**
    Create a small test: build a project that uses `Ptr(i32).cast(&x)` and verify it
    compiles and the generated Zig is correct (`&x` for Ptr, `@as([*]T, @ptrCast(&x))`
    for RawPtr).

    NOTE: This task requires investigation — the exact MIR annotator location depends on
    how method calls on generic types traverse the annotation pipeline. Read the code
    carefully. The research suggests checking ~line 1517 in mir.zig for the ptr_expr
    creation logic.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `zig build test` exits 0
    - A minimal test project using `Ptr(i32).cast(&x)` compiles successfully with `orhon build`
    - The generated Zig for `Ptr(i32).cast(&x)` produces `&x` (same as `Ptr(i32, &x)`)
    - The old syntax `Ptr(i32, &x)` still compiles correctly (backward compatibility)
  </acceptance_criteria>
  <done>MIR annotator recognizes Ptr(T).cast(&x) and RawPtr(T).cast(&x) as ptr_expr nodes, both old and new syntax work</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tester.orh and example module to .cast() syntax</name>
  <files>test/fixtures/tester.orh, src/templates/example/data_types.orh</files>
  <read_first>
    - test/fixtures/tester.orh (lines 685-710 — the pointer test section with Ptr/RawPtr usage)
    - src/templates/example/data_types.orh (line 85 — Ptr constructor)
  </read_first>
  <action>
    **In `test/fixtures/tester.orh`**, change the three pointer constructor calls:

    Line 692: `const raw: RawPtr(i32) = RawPtr(i32, &x)`
    Change to: `const raw: RawPtr(i32) = RawPtr(i32).cast(&x)`

    Line 699: `const raw: RawPtr(i32) = RawPtr(i32, &arr)`
    Change to: `const raw: RawPtr(i32) = RawPtr(i32).cast(&arr)`

    Line 705: `const p: Ptr(i32) = Ptr(i32, &x)`
    Change to: `const p: Ptr(i32) = Ptr(i32).cast(&x)`

    **In `src/templates/example/data_types.orh`**, change:

    Line 85: `const p: Ptr(i32) = Ptr(i32, &x)`
    Change to: `const p: Ptr(i32) = Ptr(i32).cast(&x)`

    After changes, run `./testall.sh` to verify the tester binary still produces correct
    output (test/10_runtime.sh checks tester output) and the example module compiles
    (test/05_compile.sh and test/09_language.sh check example module).
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Ptr(i32, &' test/fixtures/tester.orh` returns 0 (no old-style Ptr constructors)
    - `grep -c 'RawPtr(i32, &' test/fixtures/tester.orh` returns 0 (no old-style RawPtr constructors)
    - `grep -c '.cast(' test/fixtures/tester.orh` returns 3
    - `grep -c '.cast(' src/templates/example/data_types.orh` returns 1
    - `grep -c 'Ptr(i32, &' src/templates/example/data_types.orh` returns 0
    - `./testall.sh` exits 0 (all 11 test stages pass)
  </acceptance_criteria>
  <done>All Ptr/RawPtr constructors in tester and example module use .cast() method-style. No bare type-as-value construction remains. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `./testall.sh` passes all 11 stages
- `grep -r 'Ptr(.*,' test/fixtures/tester.orh src/templates/example/` returns no bare constructor calls
- `grep -r '.cast(' test/fixtures/tester.orh src/templates/example/` returns 4 total matches
- Old syntax still works (backward compatible)
</verification>

<success_criteria>
- Ptr(T).cast(&x) and RawPtr(T).cast(&x) are recognized by the MIR annotator
- All pointer constructor sites in tester.orh and example module use .cast()
- Full test suite green including runtime correctness checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-error-safety/02-03-SUMMARY.md`
</output>
