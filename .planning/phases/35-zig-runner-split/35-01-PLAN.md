---
phase: 35-zig-runner-split
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zig_runner.zig
  - src/zig_runner_build.zig
  - src/zig_runner_multi.zig
  - src/zig_runner_discovery.zig
  - build.zig
autonomous: true
requirements: [SPLIT-05, SPLIT-02]

must_haves:
  truths:
    - "zig_runner.zig is reduced to ~400 lines containing only ZigRunner struct, invocation logic, and re-exports"
    - "buildZigContent and its 4 helper functions live in zig_runner_build.zig"
    - "buildZigContentMulti and MultiTarget live in zig_runner_multi.zig"
    - "findZig, findZigInPath, zigBinaryName live in zig_runner_discovery.zig"
    - "All 266 tests pass with zero behavior change"
    - "All 16 zig_runner unit tests pass in their new file locations"
  artifacts:
    - path: "src/zig_runner.zig"
      provides: "ZigRunner struct + re-export facade"
      contains: "pub const MultiTarget"
    - path: "src/zig_runner_build.zig"
      provides: "Single-target build.zig generation + shared helpers"
      contains: "pub fn buildZigContent"
    - path: "src/zig_runner_multi.zig"
      provides: "Multi-target build.zig generation"
      contains: "pub fn buildZigContentMulti"
    - path: "src/zig_runner_discovery.zig"
      provides: "Zig binary discovery"
      contains: "pub fn findZig"
    - path: "build.zig"
      provides: "Test file registration for new files"
      contains: "zig_runner_build.zig"
  key_links:
    - from: "src/zig_runner.zig"
      to: "src/zig_runner_build.zig"
      via: "const _zig_runner_build = @import"
      pattern: "_zig_runner_build"
    - from: "src/zig_runner.zig"
      to: "src/zig_runner_multi.zig"
      via: "const _zig_runner_multi = @import"
      pattern: "_zig_runner_multi"
    - from: "src/zig_runner_multi.zig"
      to: "src/zig_runner_build.zig"
      via: "import for shared helpers (emitLinkLibs, emitIncludePath, emitCSourceFiles)"
      pattern: "_build\\.emitLinkLibs"
---

<objective>
Split zig_runner.zig (1952 lines) into 4 focused files following the established flat-file split pattern from Phases 29/32/33/34.

Purpose: Reduce file size for maintainability — no single file exceeds ~600 lines, each file has one clear responsibility.
Output: 4 files (zig_runner.zig facade + 3 satellite files), build.zig updated, all 266 tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-zig-runner-split/35-CONTEXT.md
@.planning/phases/35-zig-runner-split/35-RESEARCH.md

@src/zig_runner.zig
@build.zig
@src/pipeline.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract satellite files and create zig_runner.zig re-export facade</name>
  <files>src/zig_runner_build.zig, src/zig_runner_multi.zig, src/zig_runner_discovery.zig, src/zig_runner.zig</files>
  <read_first>
    - src/zig_runner.zig (the file being split — read ALL of it to understand exact boundaries)
    - src/pipeline.zig (sole caller — verify which symbols it accesses from zig_runner)
    - src/mir.zig (reference for re-export facade pattern from Phase 33)
    - build.zig (will be updated in Task 2)
  </read_first>
  <action>
    Extract three satellite files from zig_runner.zig per D-01 through D-10:

    **1. Create `src/zig_runner_discovery.zig` (~50 lines) per D-06:**
    - Move `findZig()` (pub fn, lines ~1645-1664), `findZigInPath()` (private fn, lines ~1666-1684), `zigBinaryName()` (private fn, lines ~1686-1688)
    - Move `const builtin = @import("builtin");` (line ~1690) to this file's imports
    - Add standard imports at top: `const std = @import("std");` and `const builtin = @import("builtin");`
    - Move the 1 test block: `test "zig runner - find zig path format"`
    - `findZig` stays `pub fn`, the other two stay private `fn`

    **2. Create `src/zig_runner_build.zig` (~560 lines) per D-04 and D-07:**
    - Move `buildZigContent()` (pub fn, lines ~477-830ish) and all its inline helper functions
    - Move `emitLinkLibs` — change from `fn` to `pub fn` (needed by zig_runner_multi.zig)
    - Move `emitIncludePath` — change from `fn` to `pub fn` (needed by zig_runner_multi.zig)
    - Move `emitCSourceFiles` — change from `fn` to `pub fn` (needed by zig_runner_multi.zig)
    - Move `generateSharedCImportFiles` (already pub fn) — also needed by ZigRunner.buildAll in runner core
    - Add imports at top: `const std = @import("std");`, `const errors = @import("errors.zig");`, `const cache = @import("cache.zig");`, `const module = @import("module.zig");` — include whatever the moved functions actually reference
    - Move all ~8 `buildZigContent` test blocks to this file

    **3. Create `src/zig_runner_multi.zig` (~593 lines) per D-05:**
    - Move `MultiTarget` struct (pub, lines ~1030-1043)
    - Move `buildZigContentMulti()` (pub fn, lines ~1047-1640)
    - Add import for shared helpers: `const _build = @import("zig_runner_build.zig");`
    - Replace all direct calls to `emitLinkLibs(...)` with `_build.emitLinkLibs(...)`
    - Replace all direct calls to `emitIncludePath(...)` with `_build.emitIncludePath(...)`
    - Replace all direct calls to `emitCSourceFiles(...)` with `_build.emitCSourceFiles(...)`
    - Replace all direct calls to `generateSharedCImportFiles(...)` with `_build.generateSharedCImportFiles(...)`
    - Add other needed imports: `const std = @import("std");`, `const errors = @import("errors.zig");`, `const cache = @import("cache.zig");`, `const module = @import("module.zig");`
    - Move all ~7 `buildZigContentMulti` test blocks to this file

    **4. Reduce `src/zig_runner.zig` to ~400 lines per D-03:**
    - Keep: `ZigResult` struct, `ZigRunner` struct with init/deinit/buildAll/build/buildLib/runTests/generateBuildZig methods, `writeTestOutput` private function
    - Keep: the 2 `formatTestOutput` test blocks
    - Add underscore-prefixed imports (per Phase 33 lesson):
      ```
      const _zig_runner_build = @import("zig_runner_build.zig");
      const _zig_runner_multi = @import("zig_runner_multi.zig");
      const _zig_runner_discovery = @import("zig_runner_discovery.zig");
      ```
    - Add re-exports for backward compatibility with pipeline.zig:
      ```
      pub const MultiTarget = _zig_runner_multi.MultiTarget;
      pub const buildZigContent = _zig_runner_build.buildZigContent;
      pub const buildZigContentMulti = _zig_runner_multi.buildZigContentMulti;
      pub const findZig = _zig_runner_discovery.findZig;
      pub const generateSharedCImportFiles = _zig_runner_build.generateSharedCImportFiles;
      ```
    - Update ZigRunner.init() to call `_zig_runner_discovery.findZig(...)` instead of `findZig(...)`
    - Update ZigRunner.buildAll() to call `_zig_runner_build.generateSharedCImportFiles(...)` instead of `generateSharedCImportFiles(...)`
    - Update ZigRunner.generateBuildZig() (or the private method it calls) to call `_zig_runner_build.buildZigContent(...)` and `_zig_runner_multi.buildZigContentMulti(...)` instead of direct calls
    - Remove all moved code (functions, structs, tests)
    - Remove `const builtin = @import("builtin");` if it was only needed by findZigInPath

    **Critical checks during extraction:**
    - Verify every `fn` that becomes cross-file is changed to `pub fn` in the satellite
    - Verify no local variable names in zig_runner_multi.zig shadow `_build` import name
    - Verify all imports each satellite file needs are present (std, errors, cache, module, etc.)
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && zig build test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/zig_runner_discovery.zig exists and contains `pub fn findZig(`
    - src/zig_runner_discovery.zig contains `const builtin = @import("builtin")`
    - src/zig_runner_discovery.zig contains `test "zig runner - find zig path format"`
    - src/zig_runner_build.zig exists and contains `pub fn buildZigContent(`
    - src/zig_runner_build.zig contains `pub fn emitLinkLibs(` (was private, now pub)
    - src/zig_runner_build.zig contains `pub fn emitIncludePath(` (was private, now pub)
    - src/zig_runner_build.zig contains `pub fn emitCSourceFiles(` (was private, now pub)
    - src/zig_runner_build.zig contains `pub fn generateSharedCImportFiles(`
    - src/zig_runner_build.zig contains `test "buildZigContent`
    - src/zig_runner_multi.zig exists and contains `pub fn buildZigContentMulti(`
    - src/zig_runner_multi.zig contains `pub const MultiTarget`
    - src/zig_runner_multi.zig contains `_build.emitLinkLibs` (cross-file helper call)
    - src/zig_runner_multi.zig contains `test "buildZigContentMulti`
    - src/zig_runner.zig contains `pub const MultiTarget = _zig_runner_multi.MultiTarget`
    - src/zig_runner.zig contains `pub const buildZigContent = _zig_runner_build.buildZigContent`
    - src/zig_runner.zig contains `pub const findZig = _zig_runner_discovery.findZig`
    - src/zig_runner.zig does NOT contain `pub fn buildZigContent(` (moved out)
    - src/zig_runner.zig does NOT contain `pub fn buildZigContentMulti(` (moved out)
    - src/zig_runner.zig does NOT contain `pub fn findZig(` (moved out)
    - src/zig_runner.zig does NOT contain `fn findZigInPath(` (moved out)
    - src/zig_runner.zig is under 500 lines (target ~400)
    - `zig build test` exits 0
  </acceptance_criteria>
  <done>All 3 satellite files created with correct content, zig_runner.zig reduced to re-export facade + ZigRunner struct, `zig build test` passes</done>
</task>

<task type="auto">
  <name>Task 2: Register new files in build.zig and run full test suite</name>
  <files>build.zig</files>
  <read_first>
    - build.zig (to find exact location of test_files array and "src/zig_runner.zig" entry)
  </read_first>
  <action>
    **1. Update build.zig test_files array:**
    Find the line `"src/zig_runner.zig",` in the test_files array (currently line 70). Add the 3 new files immediately after it:
    ```
    "src/zig_runner.zig",
    "src/zig_runner_build.zig",
    "src/zig_runner_discovery.zig",
    "src/zig_runner_multi.zig",
    ```
    Keep `src/zig_runner.zig` in the list (it still has 2 formatTestOutput tests).

    **2. Run full test suite:**
    Execute `./testall.sh` and verify all 266 tests pass with zero failures. This confirms SPLIT-02 (zero behavior change gate).

    **3. Verify file line counts:**
    Run `wc -l src/zig_runner*.zig` and confirm:
    - zig_runner.zig: under 500 lines
    - zig_runner_build.zig: roughly 500-600 lines
    - zig_runner_multi.zig: roughly 550-650 lines
    - zig_runner_discovery.zig: roughly 40-60 lines
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - build.zig contains `"src/zig_runner_build.zig",`
    - build.zig contains `"src/zig_runner_discovery.zig",`
    - build.zig contains `"src/zig_runner_multi.zig",`
    - `./testall.sh` exits 0 with all 266 tests passing
    - `wc -l src/zig_runner.zig` shows under 500 lines
    - No file in `src/zig_runner*.zig` exceeds 700 lines
  </acceptance_criteria>
  <done>build.zig updated with 3 new test file entries, full test suite passes all 266 tests, zig_runner.zig confirmed under 500 lines</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. `ls src/zig_runner*.zig | wc -l` = 4 files
2. `wc -l src/zig_runner*.zig` — no file over 700 lines, zig_runner.zig under 500
3. `zig build test` — all unit tests pass in new locations
4. `./testall.sh` — all 266 tests pass (SPLIT-02 gate)
5. `grep -c 'test "' src/zig_runner*.zig` — total = 16 (no tests lost)
6. pipeline.zig requires zero changes (re-export facade provides backward compatibility)
</verification>

<success_criteria>
- zig_runner.zig reduced to ~400 lines (ZigRunner struct + re-exports only)
- 3 new satellite files created: zig_runner_build.zig, zig_runner_multi.zig, zig_runner_discovery.zig
- All 16 unit tests relocated to the file containing the code they test
- All 266 tests pass via `./testall.sh`
- pipeline.zig unchanged (backward compatibility via re-exports)
- No generated build.zig output changes (pure refactor)
</success_criteria>

<output>
After completion, create `.planning/phases/35-zig-runner-split/35-01-SUMMARY.md`
</output>
