module main

main.name = "{s}"
main.version = Version(1, 0, 0)
main.build = build.exe
main.bitsize = 32

import std::console

func main() void {
    console.println("hello kodr !")
    console.printPrefixed("info", "ready")
}
